import theme from "../../styles/theme";

const styles = {
    code: {
        ...theme.blockSpace,
        padding: '20px',
        ...theme.codeBlock,
        fontFamily: '"Roboto Mono"',
        padding: '0 5px'
    }
};

export default styles;
