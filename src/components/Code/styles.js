import theme from "../../styles/theme";

const styles = {
    code: {
        ...theme.blockSpace,
        padding: '20px',
        ...theme.codeBlock,
        fontFamily: '"Roboto Mono"',
        padding: '5px 10px'
    }
};

export default styles;
